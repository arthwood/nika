%h2
  New Photo Set

- form_remote_for(:photo_set, @photo_set, :url => {:action => :create_photo_set}, :after => "$('new_photoset_form').innerHTML = ''; $('new_photoset_form').hide()") do |f|
  %p
    %label{:for => :photo_set_name}
      Name:
    = f.text_field :name
  %p
    %label{:for => :photo_set_description}
      Description:
    %br/
    = f.text_area :description, :cols => 50, :rows => 4
  %p
    = submit_tag "Create"
    = button_to_function 'Cancel', "$('new_photoset_form').innerHTML = ''; $('new_photo_set_link').show(); $('new_photoset_form').hide()"